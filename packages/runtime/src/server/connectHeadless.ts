// Stub: headless connect runner. Throws if invoked.
export async function runConnectHeadless(
  _config: unknown,
  _prompt?: string,
  _outputFormat?: string,
  _interactive?: boolean,
): Promise<never> {
  throw new Error("connect headless is not available in this build");
}
