// Stub: webhook payload sanitizer. Identity in external builds.
export function sanitizeWebhookPayload<T>(value: T): T {
  return value;
}

export function sanitizeInboundWebhookContent<T>(value: T): T {
  return value;
}
